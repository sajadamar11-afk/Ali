# 📱 نسخة الموبايل — صانع النماذج الامتحانية

هذا المجلد **مستقل بالكامل**. ما يحتاج أي ملف من برّا.

```
mobile_app/
├── main.py           ← ملف الدخول (شغّل هذا)
├── paper.py          ← محرك الورقة
├── word_export.py    ← محرك تصدير Word
├── sample.exam       ← مشروع جاهز للتجربة
└── requirements.txt
```

---

## تجربة سريعة على الحاسبة

```bat
cd mobile_app
pip install flet
flet run main.py
```

## نشر PWA على GitHub Pages

```bat
cd mobile_app
flet build web
```

يطلع مجلد `build\web` — ارفع **محتواه** (مو المجلد نفسه) إلى الريبو مالك، بعدين:

`Settings → Pages → Branch: main → /(root) → Save`

افتح الرابط بالآيفون → **مشاركة ← إضافة إلى الشاشة الرئيسية** → يصير تطبيق.

> بايثونك يشتغل داخل المتصفح (Pyodide)، ما يحتاج سيرفر ولا استضافة مدفوعة.

## APK لأندرويد

```bat
flet build apk
```

الملف يطلع بـ `build\apk`.

---

## داخل التطبيق

| الزر | الشغلة |
|---|---|
| 🖨 معاينة وطباعة (PDF / A4) | يفتح الورقة بتبويب جديد → **مشاركة ← طباعة ← حفظ بصيغة PDF** |
| Word | ينزّل ملف `.docx` قابل للتعديل |
| 💾 | يحفظ المشروع داخل الجهاز |
| 📂 | يسترجع آخر مشروع محفوظ |

---

## ملاحظة مهمة

لا تخلي مسار المجلد بيه أحرف عربية أو مسافات وقت `flet build` —
بيلد Flutter يوقّع بيها. خلّي المسار مثلاً `C:\projects\mobile_app`.
