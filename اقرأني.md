# 📱 صانع النماذج الامتحانية — نسخة الموبايل (PWA)

ما تحتاج بيلد، ولا Flutter، ولا انتظار. ترفع ٤ ملفات ويشتغل فوراً.

## الملفات

| الملف | الشغلة |
|---|---|
| `index.html` | التطبيق كامل (الواجهة + محرك الورقة + مصدّر Word) بملف واحد |
| `manifest.json` | يخلّيه تطبيق قابل للتثبيت على الشاشة الرئيسية |
| `sw.js` | يشغّله بدون نت (الشبكة أولاً، فما تعلق نسخة قديمة) |
| `icon.png` | أيقونة التطبيق |

## النشر (من التلفون، ٢ دقيقة)

1. ريبو جديد على GitHub (مثلاً `asila`) → Public
2. `Add file → Upload files` → ارفع الأربعة كلهم بجذر الريبو → Commit
3. `Settings → Pages → Source: **Deploy from a branch** → main → /(root)` → Save
4. بعد دقيقة افتح `https://<حسابك>.github.io/asila/`
5. سفاري → **مشاركة ← إضافة إلى الشاشة الرئيسية**

> انتبه: هنا Source = **Deploy from a branch** مو GitHub Actions.

## التعديل لاحقاً

افتح `index.html` من الريبو → ✏️ → عدّل → Commit.
التحديث يوصل خلال دقيقة (مو ١٥ دقيقة بيلد).

## الطباعة PDF

زر **معاينة وطباعة** يفتح الورقة بحجمها الحقيقي، وزر **طباعة** داخل المعاينة
يفتح شاشة الطباعة مباشرة → اختار **حفظ بصيغة PDF**.
الورقة تنطبع A4 لحالها لأن CSS فيها `@page size:A4`.

## Word

زر **Word** ينزّل `.docx` قابل للتعديل. مبني داخل المتصفح بدون أي مكتبة —
حتى ضاغط ZIP مكتوب بالملف نفسه.

## ليش تركنا Flet؟

Flet-web يشغّل بايثون داخل صندوق معزول فوق Flutter. النتيجة كانت:
واجهة برمجية تتغيّر بين الإصدارات، تخزين عنيد بالمتصفح، وصول محدود لأدوات
الصفحة (`document` / `window`)، حظر النوافذ من سفاري، وكل تجربة ١٥ دقيقة بيلد.

هنا الكود يشتغل مباشرة بالصفحة: الطباعة والتنزيل والتخزين كلها أدوات أصلية
بالمتصفح، والنشر فوري.

**نسخة الحاسبة (بايثون) ما تغيّرت** — بعدها تشتغل بنفس المحرك ونفس المخرجات.
