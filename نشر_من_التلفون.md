# 📱 النشر من التلفون — بدون حاسبة إطلاقاً

GitHub يسوي البيلد بدالك على سيرفراته. أنت ترفع الملفات مرة وحدة وخلص.

---

## الخطوات (كلها من سفاري بالآيفون)

### ١ — سوّي ريبو جديد
افتح `github.com` → `+` → **New repository**
- الاسم: `exam` (احفظه، راح يصير جزء من الرابط)
- خلّيه **Public**
- علّم **Add a README file**
- **Create repository**

### ٢ — ارفع الملفات
داخل الريبو: **Add file → Upload files**

ارفع هذي الملفات **بجذر الريبو** (مو داخل مجلد):

```
main.py
paper.py
word_export.py
pyproject.toml
```

اضغط **Commit changes**.

### ٣ — ارفع ملف البيلد
هذا لازم يكون بمسار محدد بالضبط:

**Add file → Create new file**، وبخانة الاسم اكتب:

```
.github/workflows/deploy.yml
```

> أول ما تكتب `/` بعد `.github` راح ينفتح مجلد لحاله — هذا طبيعي.

الصق محتوى `deploy.yml` كامل → **Commit changes**.

### ٤ — شغّل Pages
`Settings → Pages → Source: **GitHub Actions**`

(مو "Deploy from a branch" — اختار **GitHub Actions**)

### ٥ — انتظر
روح لتبويب **Actions**. راح تشوف عملية تشتغل.
أول مرة تأخذ **٨-١٥ دقيقة** لأنه ينزّل Flutter. المرات الجاية أسرع.

لمن تصير ✅ خضراء، افتح:

```
https://sajadamar11-afk.github.io/exam/
```

### ٦ — خلّيه تطبيق
بسفاري: **مشاركة → إضافة إلى الشاشة الرئيسية**

صار عندك أيقونة مثل أي تطبيق، ويشتغل بدون نت.

---

## تعديل البرنامج لاحقاً — كله من التلفون

افتح `main.py` من الريبو → أيقونة القلم ✏️ → عدّل → **Commit changes**.

GitHub يعيد البيلد والنشر **تلقائياً**. بعد بضع دقائق حدّث التطبيق بالتلفون وتشوف التعديل.

---

## إذا الصفحة طلعت بيضاء

٩٩٪ السبب: اسم الريبو مو `exam`.

`--base-url` بملف `deploy.yml` ياخذ اسم الريبو تلقائياً، بس إذا غيّرت الاسم بعد
ما نشرت، شغّل البيلد من جديد: **Actions → بناء ونشر التطبيق → Run workflow**.

## إذا البيلد فشل ❌

افتح العملية بتبويب Actions واقرا آخر سطر أحمر. أشهر سببين:
- ملف `main.py` مو بجذر الريبو (داخل مجلد بالغلط)
- `Settings → Pages → Source` مو مضبوط على **GitHub Actions**

---

## الملفات وين تروح

| الشغلة | وين ينحفظ |
|---|---|
| زر PDF | يفتح تبويب جديد → مشاركة → طباعة → حفظ بصيغة PDF (بملفاتك) |
| زر Word | ينزّل `.docx` بمجلد التنزيلات مال التلفون |
| زر 💾 | يخزن داخل التطبيق نفسه (يبقى حتى لو سكّرته) |
